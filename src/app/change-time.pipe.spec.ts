import { ChangeTimePipe } from './change-time.pipe';

describe('ChangeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
